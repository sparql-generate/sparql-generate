BASE <http://example.org/> 
PREFIX rqg-ite: <http://w3id.org/sparql-generate/ite/>
PREFIX rqg-fn: <http://w3id.org/sparql-generate/fn/>
PREFIX country:<http://loc.example.com/city/>
PREFIX schema: <http://schema.org/>
PREFIX wgs84_pos: <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX gn: <http://www.geonames.org/ontology#>

GENERATE { 
    [] schema:val ?position
}
SOURCE <http://example.com/list.json> AS ?source
ITERATOR rqg-ite:JSONElement(?source,"$.att[*]") AS ?key
WHERE {
    BIND(rqg-fn:JSONPath(?key, "$.position" ) AS ?position)
}